# CLAUDE.md

本项目的全部工作指南（项目概览、常用命令、核心约定、已知陷阱、计划 → 评审 → 实施工作流程、样式速查、验证方法）统一维护在 `AGENTS.md`。本文件直接引入，不另存一份：

@AGENTS.md

## Claude Code 补充

- **权威源是 `AGENTS.md`**：项目规则只改那一份，本文件保持为指针，避免两处内容漂移。
- **评审用子代理做独立评审**：本仓库的「计划 → 评审 → 实施」流程中，方案评审应交给不共享主对话上下文的子代理（fresh context）执行。设计者自评会默认自己的假设成立，独立视角才查得出可用性/安全类硬伤；评审结论按项目约定写入 `docs/review/`。
- 仅与 Claude Code 相关、不适合放进 `AGENTS.md` 的经验教训，追加在本节。
