node_modules/
*.log
.DS_Store
bin/plantuml.jar

# 转换产物（clean/ docx/ .mermaid/ .plantuml/，运行时重新生成，勿提交）
# 覆盖 md/output/ 以及测试用例旁的 md/**/output/（如 md/qa/output/）
md/output/
md/**/output/

# HTTP 服务运行时数据目录（作业目录、server-url.txt 等，运行时生成，勿提交）
data/

# 离线分发包构建产物（由 scripts/build-windows-bundle.js 生成，体积数百 MB，勿提交）
dist/
